const form=document.getElementById("accountForm"),tabs=document.querySelectorAll(".tab"),nameField=document.getElementById("name-field"),nameInput=document.getElementById("customer-name"),emailInput=document.getElementById("customer-email"),passwordInput=document.getElementById("customer-password"),loginOptions=document.getElementById("login-options"),submitButton=document.getElementById("submit-button"),formTitle=document.getElementById("form-title"),formSubtitle=document.getElementById("form-subtitle"),message=document.getElementById("form-message"),otpField=document.getElementById("otp-field"),otpInput=document.getElementById("customer-otp"),otpMessage=document.getElementById("otp-message");let mode="login",otpVerified=false;
function setMessage(text,success=false){message.textContent=text;message.classList.toggle("success",success)}
function setMode(next){mode=next;const registering=mode==="register";nameField.hidden=!registering;nameInput.required=registering;loginOptions.hidden=registering;formTitle.textContent=registering?"Create your jewellery room":"Enter your jewellery room";formSubtitle.textContent=registering?"Create an account and verify your email with a one-time code.":"Sign in to save favourites, follow orders and receive private collection previews.";submitButton.innerHTML=registering?(otpVerified?"Create my account <span>→</span>":"Send verification code <span>→</span>"):"Enter my account <span>→</span>";tabs.forEach(t=>{const active=t.dataset.mode===mode;t.classList.toggle("active",active);t.setAttribute("aria-selected",String(active))});setMessage("");passwordInput.autocomplete=registering?"new-password":"current-password";if(otpField)otpField.hidden=!registering||!otpVerified}
tabs.forEach(t=>t.addEventListener("click",()=>setMode(t.dataset.mode)));document.querySelector(".password-toggle").addEventListener("click",e=>{const visible=passwordInput.type==="text";passwordInput.type=visible?"password":"text";e.currentTarget.textContent=visible?"Show":"Hide"});document.getElementById("forgot-password").addEventListener("click",()=>setMessage(emailInput.value?`Password reset instructions can be sent to ${emailInput.value}.`:"Enter your email address first and we will help you reset your password."));
form.addEventListener("submit",e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}const account={name:nameInput.value.trim(),email:emailInput.value.trim().toLowerCase(),memberSince:new Date().getFullYear()};if(mode==="register"&&!otpVerified){otpVerified=true;if(otpField){otpField.hidden=false;otpInput.required=true;otpMessage.textContent="Demo code: 123456 (connect an email/SMS provider for production)."}submitButton.innerHTML="Verify code <span>→</span>";setMessage(`A verification code has been sent to ${account.email}.`,true);return}if(mode==="register"){if(otpInput&&otpInput.value!=="123456"){otpMessage.textContent="Enter the demo code 123456 to continue.";return}localStorage.setItem("vimlaCustomer",JSON.stringify(account));setMessage("Your private jewellery account has been created. Welcome to Vimla.",true);setTimeout(()=>{window.location.href="dashboard.html"},500);return}const saved=JSON.parse(localStorage.getItem("vimlaCustomer")||"null");if(saved&&saved.email===account.email){localStorage.setItem("vimlaCustomer",JSON.stringify({...saved,lastLogin:new Date().toISOString()}));const target=new URLSearchParams(window.location.search).get("return");window.location.href=target==="checkout"?"index.html#checkout":"dashboard.html"}else setMessage("For this preview, create an account first or connect this form to your secure customer database.")});setMode("login");
